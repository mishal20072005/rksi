#include <iostream>

int main() {
  int sum = 0;
  for (int i = 2; i <= 100; i += 2) {
    sum += i;
    if (sum > 1000) {
      break;
    }
    std::cout << i << std::endl;
  }
  std::cout << sum << std::endl;
  return 0;
}