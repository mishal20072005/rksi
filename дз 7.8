#include <iostream>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    char stroka[] = { "AaBbCcDdEeFfGgggggg" };
    int a = 0, b = 0;
    for (int i = 0; i < 20; i++)
    {
        if ((stroka[i] > 'A') && (stroka[i] < 'Z'))a++;
    }
    cout << "количество заглавных букв: " << a << endl;
    getwchar();
    return 0;
}
